#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main(int argc, char *argv[]){
	int opt;
	int n;
	int m;
	
	while((opt = getopt(argc, argv, "n:m")) !=-1){
		switch(op;t){
			:
		}
	}
}
